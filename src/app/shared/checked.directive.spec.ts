import { CheckedDirective } from './checked.directive';

describe('CheckedDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckedDirective();
    expect(directive).toBeTruthy();
  });
});
